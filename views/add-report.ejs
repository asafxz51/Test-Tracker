<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Add New Test Case - Test Tracker</title>
    <link rel="stylesheet" href="/css/styles.css"> <!-- Your main stylesheet -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        /*
            NEW STYLES FOR THE CORRECT LAYOUT
            You can move this to your main styles.css file.
        */
        body {
            background-color: #f4f7f9;
            font-family: Arial, sans-serif;
        }

        .form-container {
            max-width: 800px;
            margin: 40px auto;
            padding: 30px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .form-container h1 {
            margin-bottom: 30px;
            font-size: 24px;
        }

        /* Default layout for form groups: label on top */
        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            /* Ensures label is on its own line */
            font-weight: bold;
            margin-bottom: 8px;
            /* Space between label and input */
        }

        .form-group input[type="text"],
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        /* --- SPECIAL LAYOUTS USING FLEXBOX --- */

        /* A row container for side-by-side elements */
        .form-row {
            display: flex;
            gap: 20px;
            /* Space between items in a row */
            align-items: flex-start;
            /* Aligns items to the top */
        }

        .form-row .form-group {
            flex-grow: 1;
            /* Allows items to grow and fill the space */
            width: 100%;
            /* Important for flex items */
        }

        /* --- STYLES FOR THE DYNAMIC STEPS (PERFECT ALIGNMENT) --- */
        .step-input-group {
            display: flex;
            align-items: center;
            /* THIS IS THE FIX for vertical alignment */
            gap: 10px;
            margin-bottom: 10px;
        }

        .step-number {
            font-weight: bold;
        }

        .step-input-group input[type="text"] {
            flex-grow: 1;
            /* Makes the input take up all the space */
        }

        .remove-step-btn {
            flex-shrink: 0;
            font-weight: bold;
            background-color: #e74c3c;
            color: white;
            border: none;
            border-radius: 50%;
            width: 24px;
            height: 24px;
            padding: 0;
            font-size: 14px;
            line-height: 24px;
            cursor: pointer;
            text-align: center;
        }

        /* Final button styling */
        .form-actions {
            margin-top: 30px;
        }
    </style>
</head>

<body>
    <div class="form-container">
        <h1>Add New Test Case</h1>

        <form action="/add" method="POST">

            <!-- Row 1: Title and Description -->
            <div class="form-row">
                <div class="form-group" style="width: 40%;"> <!-- Title takes less space -->
                    <label for="title">Title</label>
                    <input type="text" id="title" name="title" required>
                </div>
                <div class="form-group">
                    <label for="description">Description</label>
                    <textarea id="description" name="description" rows="4"></textarea>
                </div>
            </div>

            <!-- Row 2: Dropdowns -->
            <div class="form-row">
                <div class="form-group">
                    <label for="type">Type</label>
                    <select id="type" name="type">
                        <option selected>Functional</option>
                        <option>Regression</option>
                        <option>Smoke</option>
                        <option>UI</option>
                        <option>API</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="priority">Priority</label>